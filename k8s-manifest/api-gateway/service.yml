apiVersion: v1
kind: Service

metadata:
  name: api-gateway-service
  namespace: ecommerce
spec:
  type: NodePort
  selector:
    app: api-gateway-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007
