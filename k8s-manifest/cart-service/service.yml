apiVersion: v1
kind: Service

metadata:
  name: cart-service
  namespace: ecommerce

spec:

  selector:
    app: cart-service-app
  
  type: ClusterIP
  
  ports:
    - port: 3003
      targetPort: 3003
