apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: ecommerce

spec:
  selector:
    app: order-service-app

  type: ClusterIP

  ports:
    - port: 3004
      targetPort: 3004

